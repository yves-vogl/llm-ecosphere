# Top-level pin for the Semgrep SAST toolchain (sast.yml workflow).
# The hash-pinned lockfile is generated from this file:
#   uv pip compile --generate-hashes --python-version 3.12 -o requirements/sast.txt requirements/sast.in
semgrep==1.174.0
